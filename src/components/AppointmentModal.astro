---
import { Image } from 'astro:assets';
import logo from '../assets/logo.jpg';
---

<div id="booking-modal" class="fixed inset-0 z-[100] hidden" aria-labelledby="modal-title" role="dialog" aria-modal="true">
  <!-- Backdrop -->
  <div class="fixed inset-0 bg-gray-900/75 backdrop-blur-sm transition-opacity opacity-0" id="booking-backdrop"></div>

  <div class="fixed inset-0 z-10 w-screen overflow-y-auto">
    <div class="flex min-h-full items-center justify-center p-4 text-center sm:p-0">
      
      <!-- Modal Panel -->
      <div class="relative transform overflow-hidden rounded-lg bg-white text-left shadow-xl transition-all sm:my-8 sm:w-full sm:max-w-5xl opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95" id="booking-panel">
        
        <!-- Header -->
        <div class="bg-white px-4 pb-4 pt-5 sm:p-6 sm:pb-4 border-b border-gray-100 flex justify-between items-center">
            <div class="flex items-center gap-3">
                 <Image src={logo} alt="Logo" class="h-12 w-auto object-contain" />
                 <h3 class="text-lg font-bold leading-6 text-main font-display" id="modal-title">Request Appointment</h3>
            </div>
            <button type="button" id="close-booking-btn" class="rounded-md bg-white text-description hover:text-primary focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2">
              <span class="sr-only">Close</span>
              <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
        </div>

        <!-- Iframe Container -->
        <div class="bg-white h-[650px] w-full relative">
             <div class="absolute inset-0 flex items-center justify-center text-gray-400 text-sm z-0">
                Loading scheduling system...
             </div>
             <!-- The actual widget iframe -->
             <iframe 
                src="https://www.hibuappointments.com/book/iframe/111406166?lang=en" 
                class="absolute inset-0 w-full h-full z-10 border-0"
                allow="camera; microphone; autoplay; encrypted-media;"
                title="Appointment Scheduling Widget"
                id="booking-iframe"
            ></iframe>
        </div>
        
        <!-- Footer Info -->
        <div class="bg-gray-50 px-4 py-3 sm:flex sm:flex-row-reverse sm:px-6 border-t border-gray-100">
          <p class="text-base text-main text-center w-full font-bold">
            You can also book with us directly at <a href="tel:4347911152" class="font-bold text-primary hover:underline">(434) 791-1152</a>.
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  // Elements
  const modal = document.getElementById('booking-modal');
  const backdrop = document.getElementById('booking-backdrop');
  const panel = document.getElementById('booking-panel');
  const closeBtn = document.getElementById('close-booking-btn');
  const iframe = document.getElementById('booking-iframe') as HTMLIFrameElement;
  const body = document.body;

  // Functions
  function openModal() {
    if (!modal || !backdrop || !panel) return;
    
    modal.classList.remove('hidden');
    requestAnimationFrame(() => {
        backdrop.classList.remove('opacity-0');
        panel.classList.remove('opacity-0', 'translate-y-4', 'sm:translate-y-0', 'sm:scale-95');
    });
    body.style.overflow = 'hidden'; 

    // Force iframe reload to ensure it captures the container size correctly
    if (iframe && iframe.src) {
        const currentSrc = iframe.src;
        iframe.src = '';
        iframe.src = currentSrc;
    }
  }

  function closeModal() {
    if (!modal || !backdrop || !panel) return;

    backdrop.classList.add('opacity-0');
    panel.classList.add('opacity-0', 'translate-y-4', 'sm:translate-y-0', 'sm:scale-95');
    
    setTimeout(() => {
        modal.classList.add('hidden');
        body.style.overflow = ''; 
    }, 300); 
  }

  document.addEventListener('open-booking-modal', openModal);
  closeBtn?.addEventListener('click', closeModal);
  backdrop?.addEventListener('click', closeModal); 

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && !modal?.classList.contains('hidden')) {
        closeModal();
    }
  });
</script>
